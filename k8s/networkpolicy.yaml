apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cyberminions-netpol
spec:
  podSelector:
    matchLabels:
      app: cyberminions
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector: {}
